import SwiftUI

struct MissingExample: View {

  var body: some View {
    Text("This example is unavailable for this version of the app. Make sure you are on the latest version to unlock all available examples!")
    .padding()
  }
}

struct MissingExample_Previews: PreviewProvider {
  static var previews: some View {
    MissingExample()
  }
}
